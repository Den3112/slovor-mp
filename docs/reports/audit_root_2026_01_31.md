# 🔍 Аудит файлов в корневой директории

Я проверил все файлы в корне проекта. Вот отчет:

### ✅ Все файлы на месте
Все файлы необходимы (код, конфиги, тесты, Docker).

### 🗑️ Удален мусор
**`server.log`** — удален, это лог от запусков сервера.

### ⚠️ Подозрительный файл: `proxy.ts`
В корне есть файл `proxy.ts`, который выглядит как **Middleware** для Next.js (логика языков и сессий).
Next.js его **не видит**, так как файл должен называться `middleware.ts` (или `.js`).
Если это так, то сейчас **Middleware в проекте не работает**.

**Что делать с `proxy.ts`?**
1. Переименовать в `middleware.ts` (включить)?
2. Оставить как есть (может, он отключен специально)?
3. Удалить?
